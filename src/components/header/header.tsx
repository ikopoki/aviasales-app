/* eslint-disable no-unused-vars */
import React from 'react'
import './header.scss'
import logo from '../../icons/logo.svg'

export default function Header () {
    return  (
        <img src={logo} alt='aviasales-logo' className='logo-header'/>
    )
}
